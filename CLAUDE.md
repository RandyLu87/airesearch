# Repository guidance

本仓库的工程规范正文保存在 **`AGENTS.md`**，Codex 与 Claude Code 共用同一份，不做第二份副本。

**开始任何研究、改动或提交之前，先读 `AGENTS.md`。** 其中的公司目录命名、研究快照文件名、`research/site/` 禁止手改、以及必须运行的校验命令都是硬性约束。

做公司研究时执行 `docs/research/WORKFLOW.md`（`public-company-financial-research` skill 也只是指向它的薄壳）。
